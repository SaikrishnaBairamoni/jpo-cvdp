name: cvdp
on: 
 workflow_dispatch:
    
jobs:
  cvdp:
   runs-on: ubuntu-latest
   container:
    image: ubuntu:jammy-20230126
    env:
      DEBIAN_FRONTEND: noninteractive
      INIT_ENV: "/home/jpo-cvdp/.base-image/init-env.sh"
      SONAR_SCANNER_VERSION: "4.6.2.2472"
      REDACTION_PROPERTIES_PATH: "/__w/jpo-cvdp/jpo-cvdp/config/fieldsToRedact.txt"
      BUILD_WRAPPER_OUT_DIR: "/__w/jpo-cvdp/jpo-cvdp/bw-output"
      TERM: xterm
    options: "--user root"
   steps:
     - name: Checkout ${{ github.event.repository.name }}
       uses: actions/checkout@v3   
       with:
        fetch-depth: 0
     - name: Install Latest Docker
       run: |
         apt update
         apt-get -y install sudo wget curl gnupg lsb-release gcovr unzip
         sudo apt-get -y install software-properties-common
         curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
         sudo add-apt-repository -y "deb [arch=amd64] https://download.docker.com/linux/ubuntu  $(lsb_release -cs)  stable"
         #curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
         sudo curl -L "https://github.com/docker/compose/releases/download/1.24.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
         chmod +x /usr/local/bin/docker-compose
         sudo apt-get -y update
         sudo apt-get -y install docker-ce
     - name: Check Docker Version
       run: docker --version && docker-compose version
     - name: install g++
       run: |
         sudo apt-get -y install build-essential
         sudo apt -y install cmake g++ libprotobuf-dev protobuf-compiler
         sudo apt -y install apt-transport-https ca-certificates curl software-properties-common
         export CXX="g++"
     - name: install librdkafka
       run: |
         git clone --depth 1 https://github.com/confluentinc/librdkafka.git /home/jpo-cvdp/ext/librdkafka
         cd /home/jpo-cvdp/ext/librdkafka/
         cmake -H. -B_cmake_build
         cmake --build _cmake_build
         cmake --build _cmake_build --target install
     - name: Install sonar-scanner and build-wrapper
       uses: SonarSource/sonarcloud-github-c-cpp@v1
     - name: Build and Generate test coverage
       run: |
         ls -la && pwd 
         chmod   755  -R /__w/jpo-cvdp/jpo-cvdp
         export LD_LIBRARY_PATH=/usr/local/lib
         build-wrapper-linux-x86-64 --out-dir $BUILD_WRAPPER_OUT_DIR ./build_and_run_unit_tests.sh
         ./coverage.sh        
     - name: Find .gcov
       shell: bash
       run: |
         find "$GITHUB_WORKSPACE" -name "coverage.xml"  
     - name: Archive code coverage results
       uses: actions/upload-artifact@v3
       with:
         name: cvdp-coverage
         path: /__w/jpo-cvdp/jpo-cvdp/coverage/coverage.xml
         if-no-files-found: error   
     - run: ls -la && pwd                 
  sonar:
    needs: [cvdp]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout ${{ github.event.repository.name }}
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - run: ls -la && pwd 
      - run: chmod +x -R /home/runner/work/jpo-cvdp/jpo-cvdp
      - name: Download code coverage results
        uses: actions/download-artifact@v3
        with:
          name: cvdp-coverage
          path: /__w/jpo-cvdp/jpo-cvdp/coverage/coverage.xml
      - name: Setup SonarScanner
        uses: warchant/setup-sonar-scanner@v4
        with:
          version: 4.8.0.2856
      - name: Generate sonar properties file
        run: |
          cat <<EOF > /tmp/sonar-scanner.properties
          sonar.host.url=https://sonarcloud.io
          sonar.modules=cv-lib
          sonar.projectKey=usdot-jpo-ode-1_jpo-cvdp
          sonar.organization=usdot-jpo-ode-1
          sonar.sources=src,cv-lib/src
          sonar.exclusions=src/ppm.cpp
          cv-lib.sonar.projectBaseDir=$GITHUB_WORKSPACE/__w/jpo-cvdp/jpo-cvdp/
          cv-lib.sonar.sources=src
          cv-lib.sonar.tests=test
          EOF
      - name: Run SonarScanner
        uses: usdot-fhwa-stol/actions/sonar-scanner@main
        with:
          sonar-properties-path: /tmp/sonar-scanner.properties
          sonar-token: ${{ secrets.SONAR_TOKEN }}
          working-dir: $GITHUB_WORKSPACE
